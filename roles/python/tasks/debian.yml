---
- name: Install Python 3 and necessary tools
  ansible.builtin.apt:
    pkg:
      - python3
      - python3-pip
      - python3-setuptools
  environment:
    DEBIAN_FRONTEND: noninteractive
  tags:
    - python
